services:
  web:
    image: ${WEB_IMAGE:-sewpg-bid/web:offline-latest}
    pull_policy: never

  fastapi:
    image: ${FASTAPI_IMAGE:-sewpg-bid/fastapi:offline-latest}
    pull_policy: never

  worker:
    image: ${FASTAPI_IMAGE:-sewpg-bid/fastapi:offline-latest}
    pull_policy: never

  material-worker:
    image: ${FASTAPI_IMAGE:-sewpg-bid/fastapi:offline-latest}
    pull_policy: never

  docling-worker:
    image: ${DOCLING_IMAGE:-sewpg-bid/docling-worker:offline-latest}
    pull_policy: never

  opencode:
    image: ${OPENCODE_IMAGE:-sewpg-bid/opencode:offline-latest}
    pull_policy: never

  onlyoffice:
    image: ${ONLYOFFICE_IMAGE:-sewpg-bid/onlyoffice:9.3.1.2-fontpack-v1}
    pull_policy: never

  redis:
    image: ${REDIS_IMAGE:-redis:7-alpine}
    pull_policy: never

  postgres:
    pull_policy: never

  minio:
    pull_policy: never
