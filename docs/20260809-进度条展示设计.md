# 进度条展示设计（技术标五处 + 商务标现状）

面向后续改动的索引文档：先说清分层与各处进度的**数据来源**，再给每处的跳转位置。
改进度条之前先看第 2 节判断该改哪一层——**改版式去共享卡片，改快慢去各自的折算模块，
改文案口径去后端**，三层混改是这套东西上一次跑偏的原因。

- 落地提交：`feat(技术标): 统一五处进度条展示`
- 实测样本：`PRJ-0004`（正文生成一次完整运行，见 3.2 节的耗时表）

---

## 1. 五处进度条在哪

| # | 场景 | 入口页面 | 展示组件 | 折算逻辑 |
| --- | --- | --- | --- | --- |
| 1 | 目录生成 | `TechnicalParseResult.jsx` | `TechnicalDirectoryProgressPanel` | `technicalDirectoryProgress.js` |
| 2 | 重新生成目录 | `TechnicalOutlineReview.jsx` 内的 `DirectoryGenerationProgressModal` | 同上（弹窗里嵌同一块面板） | 同上 |
| 3 | 生成技术标正文 | `TechnicalGapRecognition.jsx` | `TechnicalGenerationProgressModal` | `technicalGenerationProgress.js` |
| 4 | 重新生成正文 | `TechnicalCoCreationEditor.jsx` | 同上（同一个组件，换 `completedMessage`） | 同上 |
| 5 | 素材匹配 | `TechnicalOutlineReview.jsx` | `MaterialMatchProgressModal` | 组件内（无后端进度，见 3.3） |

1 和 2 用的是同一块面板、同一份状态解析，差别只是外面套没套弹窗；3 和 4 同理。
所以**真正需要维护的进度语义只有三套**：目录、正文、素材匹配。

---

## 2. 分层：改什么去哪一层

```
                    ┌─────────────────────────────────────────┐
                    │  BidProgressPanel.jsx                    │  ← 版式层（唯一）
                    │  图标 / 明细行 / 耗时行 / 百分比徽标 / 条纹条 │
                    └─────────────────────────────────────────┘
                          ▲              ▲              ▲
        ┌─────────────────┘              │              └──────────────────┐
        │                                │                                 │
┌───────────────┐          ┌──────────────────────┐          ┌──────────────────────┐
│ 目录进度适配   │          │ 正文进度适配          │          │ 素材匹配适配          │  ← 折算层
│ Directory     │          │ Generation           │          │ MaterialMatch        │
│ ProgressPanel │          │ ProgressModal        │          │ ProgressModal        │
└───────────────┘          └──────────────────────┘          └──────────────────────┘
        │                                │                                 │
┌───────────────────────┐  ┌──────────────────────────┐  ┌────────────────────────┐
│ technicalDirectory    │  │ technicalGeneration      │  │ （无后端进度可查）        │  ← 语义层
│ Progress.js           │  │ Progress.js              │  │  组件内按时间估算         │
└───────────────────────┘  └──────────────────────────┘  └────────────────────────┘
        │                                │
   目录生成状态接口              fill-generation 状态接口
```

**版式层** `code/sewpg-bid-frontend/src/components/shared/BidProgressPanel.jsx`

纯展示，只收五个已经算好的值，不含任何业务判断（没有 `if (是目录)` 之类的分支）：

| prop | 含义 |
| --- | --- |
| `tone` | `running` / `success` / `danger` / `neutral`，决定图标、配色 |
| `detail` | 第一行，量化明细 |
| `elapsedText` | 第二行，耗时 |
| `percentage` | 0~100，同时驱动条宽和徽标 |
| `running` | 是否挂条纹动画 `bg-stripes`（定义在 `src/index.css`） |

改「两行结构、徽标写什么、条纹要不要、字号」→ **只改这个文件，五处同时生效**。

**折算层**：三个适配组件，各自把自己那套状态翻译成上面五个 prop。
**语义层**：两个纯函数模块，无 React 依赖，可直接被 `node --test` 覆盖。

耗时文案在 `src/utils/progressDuration.js`，五处共用：
`formatProgressDuration(秒)` 出「x 分 y 秒」，`progressElapsedLine(秒, {finished})`
出「已运行 …」/「总耗时 …」。终态一律「总耗时」，包括失败。

---

## 3. 三套进度语义

### 3.1 目录（场景 1、2）

**数据来源最好，是另外两处的模板。** 后端在判定阶段回传真实计数，
所以明细行是可核对的数字而不是形容词：

> 已判定目录条款 12/54 项 · 技术附表 3/83 项

`technicalDirectoryProgress.js` 关键位置：

| 位置 | 作用 |
| --- | --- |
| `normalizeDecisionProgress` | 解析后端 `decisionProgress`，`total<=0` 直接判空，不编造分母 |
| `directoryDisplayPercentage` | **锚点 + 爬升**：后端 percentage 是锚点，锚点之间按 `CREEP_PERCENT_PER_SECOND = 0.05`（每秒 0.05%）慢爬，单次最多爬 `CREEP_MAX_PERCENT = 6`，运行期封顶 `RUNNING_DISPLAY_CAP = 96` |
| `mergeMonotonicDirectoryProgress` | 跨轮询保单调：百分比、阶段、判定计数都不许回退；同时给锚点盖时间戳 `percentageUpdatedAt` 供上面那个爬升用 |
| `directoryElapsedSeconds` | 起点 `startedAt`（缺失时退回最早一条 event），终态冻结在完成/失败时刻 |
| `summarizeDirectoryProgress` | 出 `tone` 和明细文案；`internalDirectoryTextPattern` 过滤 futurecode/opencode/S2/session 等内部名词 |

调快慢 → 改 `CREEP_*` / `RUNNING_DISPLAY_CAP`。
调文案 → 改 `decisionDetailText` / `visibleFailureSummary`。

### 3.2 正文（场景 3、4）

后端 `percentage` 的原始锚点**不能直接当进度用**。PRJ-0004 实测：

| 事件 step | 时刻 | 本段耗时 | 后端 percentage | 占总时长 |
| --- | --- | --- | --- | --- |
| `bootstrap` | 12:39:50 | 64 秒 | 5 → 30 | 22.9% |
| `inputs_ready` | 12:40:54 | 1 秒 | 30 → 60 | 0.4% |
| `assembly_waiting` | 12:40:55 | **168 秒** | 60 → 85 | **60.0%** |
| `assembling` | 12:43:43 | 2 秒 | 85 → 90 | 0.7% |
| `format_cleaning` | 12:43:45 | 44 秒 | 90 → 96 | 15.7% |
| `format_done` | 12:44:29 | 1 秒 | 96 → 100 | 0.4% |

即：**1 秒内跳 30 个点，然后 168 秒只走 25 个点**。这就是「前 60% 一分钟、
后 40% 好几分钟」的来源。

`technicalGenerationProgress.js` 的处理是按实测耗时占比重排区间，
`STAGES` 表以事件 `step` 为键：

```
bootstrap                3 → 23   预计 65 秒
inputs_ready            23 → 25   预计  3 秒
assembly_waiting        25 → 82   预计 170 秒   ← 最长的一段，给最宽的区间
assembling              82 → 85   预计  5 秒
format_cleaning         85 → 97   预计 45 秒
format_done             97 → 100  预计  3 秒
```

阶段内用 `easeWithinStage` 渐进逼近区间末端（`tau = 预计/2.5`，走到预计耗时时
约到 92%），慢于预期也不会越界，所以**不会卡死也不会提前打满**。

**组装段有真实计数时以计数为准。** 后端逐条回传 `assemblyProgress: {done, total, updatedAt}`，
明细行变成：

> 已组装目录项 27/54 项

此时百分比 = `25 + 57 × done/total`，两次计数之间只在「下一条目录项的刻度」以内
小步平滑，绝不越过。计数缺失（历史数据、商务标）时自动退回上面的阶段估算，
再退不动就用 `PERCENTAGE_ANCHORS` 把后端 percentage 映射到同一套刻度。

后端这条链路（改计数口径走这里）：

```
merger.py  merge()  正文遍历循环，逐条 progress_callback(i, total)
   ↓
runner.py  _throttled_merge_progress()   按秒节流，收尾那次必报
   ↓
tech_assembly.py  _run_assembler_manifest → _run_local_assembler
   ↓
bid_generation_flow.py  _handle_fill_progress("assembling_progress")
   ↓
bid_fill_generation_state.py  update_fill_generation_state(assembly_progress=...)
   → fill_state.assemblyProgress
```

两个约束别破坏：

- **不写 event**。`events` 是 `[-20:]` 环形缓冲，逐条写会把 `bootstrap` 和阶段历史挤掉，
  耗时基线和阶段判断都会跟着废掉。所以另记了 `fill_state.startedAt`。
- **按秒节流**。每次上报都要落库，逐条写库会把耗时算到进度汇报本身上。
- 顺带：`_is_fill_generation_stale` 已把 `assemblyProgress.updatedAt` 算进心跳，
  否则长组装会被 60 分钟无进展的自动收口误判。

### 3.3 素材匹配（场景 5）

**这处没有真实进度可查，是当前五处里唯一的估算。**
`POST /gaps-detection/run` 是一次性阻塞请求（内部跑纯脚本缺口识别，不调 LLM），
返回前没有任何中间态可轮询。改造前这里写死 `running ? 68 : 100`，是个假数字。

现在的口径写在 `MaterialMatchProgressModal.jsx`：

- 计时真实：起点是点击确认的时刻，终点是接口返回的时刻，由页面记账
  （`materialMatchProgressState.js` 的 idle/started/finished 三个取值，技术标商务标共用）。
- 百分比是估算：`EXPECTED_SECONDS = 60`，渐进逼近 `RUNNING_CAP = 92` 后停住等真实结果，
  **不封顶到 100**——没拿到返回就不许显示完成。
- 明细里的目录项数量是真的，来自当前目录 `countNodes(nodes)`。

**想让它变成真进度**，得先把这个接口改成「起任务 + 轮询状态」，
参考正文那条链路的形状：起一个后台 job、把中间计数写进项目状态、前端轮询。
在那之前，别把这里的估算当成真实进度看。

---

## 4. 已知不一致（下一步可做）

- **商务标正文进度还是旧版式。** 商务标有自己的 `BusinessGenerationProgressModal`
  和 `BusinessGapPlanProgressModal`（都定义在 `BusinessGapRecognition.jsx` 内部），
  没接共享卡片。本次按技术标任务边界停手了。要拉齐的话，工作量是照着
  `TechnicalGenerationProgressModal` 写一个商务标适配层，版式层不用动。
  注意商务标走的是 opencode skill 装配，阶段耗时分布和技术标不同，
  `STAGES` 表的区间和预计秒数要按商务标自己的实测重新量。
- **两条线共用的只有** `BidProgressPanel`、`MaterialMatchProgressModal`、
  `materialMatchProgressState`、`progressDuration`。改这四个要同时确认商务标。
- 素材匹配的估算见 3.3。

---

## 5. 测试与回归

| 文件 | 覆盖 |
| --- | --- |
| `technicalDirectoryProgress.test.mjs` | 目录判定计数、锚点爬升、跨轮询单调、耗时冻结、内部名词过滤；另有「五处共用同一张卡片」和「徽标只留百分比」的结构断言 |
| `technicalGenerationProgress.test.mjs` | 用 PRJ-0004 真实时间戳验证区间分配、单调性、组装计数优先与平滑不越界、耗时口径、文案脱敏 |
| `tests/test_fill_generation.py` | `assembling_progress` 落 `assemblyProgress` 且不写 event、分母为 0 不落数；`_throttled_merge_progress` 的节流与收尾必报 |

改进度逻辑请先跑这三个。版式层改动会被前两个文件里的结构断言拦住——
那些断言是故意留的，用来防止「徽标又加回状态词」这类回退。

命令：

```bash
# 前端
cd code/sewpg-bid-frontend && npm run lint && npm run build
node --test "src/**/*.test.mjs" "src/**/*.test.js"

# 后端（环境准备见 AGENTS.md「后端测试跑在与 CI 对齐的环境里」）
cd code/sewpg-bid-backend
APP_STORE_BACKEND=memory \
DATABASE_URL="postgresql+asyncpg://biduser:bidpass@localhost:5432/bidplatform" \
.venv/bin/python -m pytest -m "not integration" tests/test_fill_generation.py
```

> 前端全量 node 测试里 `事实表清单和素材范围变更后自动重建` 与
> `技术标 Wiki 使用 jobId 恢复轮询` 两项、后端 `test_business_fill_generation_uses_business_assembler_without_technical_gap_state`
> 与 `test_fact_specs_upload_accepts_xlsx` 两项，在本次改动前的基线上同样失败，
> 与进度条无关。
