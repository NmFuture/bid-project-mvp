# 系统性重构总 Plan（2026-08-12）

> 来源：2026-08-12 对 `docs/anbc_doc/架构总览/` 记录问题的逐项代码复核。
> 复核结论：**05-Harness 短板清单 11 项全部仍存在**（客户端巨石化反而加剧 2723→2817 行）；PeripheralStore 替换完成但旧代码未删；前端 6 项接缝中 4 项原样存在、2 项部分改善。
> 本文档是编排入口：任务拆分、依赖、波次。单项修改的实施方案见 `docs/plan/tasks/` 对应 handoff。

## 1. 目标

1. **清死代码**：删除已完成历史使命、零调用的遗留物（PeripheralStore、遗留 Skill、零调用 API 封装、孤儿端点）。
2. **收 Harness 欠账**：按 05-Harness 短板清单收敛 opencode 客户端与部署形态，优先解决「客户端可维护性、并发配置割裂、会话无回收、端口无鉴权」四点主欠账。
3. **补前端接缝**：质量警示接线、项目信息选项接真实数据源。

## 2. 任务总表

### 死代码清理（deadcode）

| id | 内容 | 优先级 | 依赖 | 波次 |
|---|---|---|---|---|
| deadcode-01 | 删除 `peripheral.py` 的 PeripheralStore 及种子数据（保留 PeripheralError/now_day） | P2 | — | 0 |
| deadcode-02 | 删除遗留 Skill `opencode/.opencode/skills/bid-draft-sections-json` | P2 | — | 0 |
| deadcode-03 | 删除前端零调用 API 封装（submitReview/recheck/tagImport*/wiki create-update-delete/forceSave） | P2 | — | 0 |
| deadcode-04 | 后端孤儿端点清理（submit-review/recheck/tag-import/wiki CRUD/force-save/export(check)），**先确认无 Skill 直调** | P2 | deadcode-03 | 1 |

### Harness 短板（harness，编号与 05-Harness §5 短板清单一一对应）

| id | 内容 | 优先级 | 依赖 | 波次 |
|---|---|---|---|---|
| harness-01 | opencode_client 拆分 + early_tool_command 注册机制（短板 1） | P0 | — | 2 |
| harness-02 | 并发治理统一：单并发预算、章节并行度配置化、代码默认与 compose 对齐（短板 2） | P0 | — | 1 |
| harness-03 | send_prompt 中途重试/断线恢复（短板 3） | P1 | harness-06 | 2 |
| harness-04 | 会话生命周期回收：DELETE session + opencode_data 卷清理（短板 4） | P1 | harness-01 | 2 |
| harness-05 | opencode 4096 端口鉴权 + 资源限额（短板 5，呼应 20260718 安全 handoff S3） | P0 | — | 1 |
| harness-06 | 客户端异步化：httpx async，去 daemon 线程（短板 6） | P1 | harness-01 | 2 |
| harness-07 | 超时配置统一：单一时超时事实源（短板 7） | P2 | — | 1 |
| harness-08 | 模型回退逻辑单点化（短板 8，现 4 处重复） | P2 | — | 1 |
| harness-09 | Skill 运行时注册机制，加 skill 免改镜像（短板 9） | P1 | — | 3 |
| harness-10 | opencode-auth 自举：文档 + auth.json 生成方式（短板 10） | P2 | — | 0 |
| harness-11 | STAGES.md 补商务轨映射（短板 11） | P2 | — | 0 |

### 前端与数据（frontend）

| id | 内容 | 优先级 | 依赖 | 波次 |
|---|---|---|---|---|
| frontend-01 | 缺口页逐条质量警示接线（复用/替代死代码 resultSummaryForItem） | P1 | — | 3 |
| frontend-02 | 项目信息选项接真实数据源（现为 static-placeholder） | P2 | **需先拍板数据源** | 3 |

## 3. 执行波次

- **波次 0（快赢，可全部并行）**：deadcode-01/02/03、harness-10、harness-11。纯删除与文档，互不冲突。
- **波次 1（配置与接缝）**：harness-08、02、07、05、deadcode-04。都碰 `config.py` / compose / `opencode_client.py` 局部，按序合入避免冲突；harness-05 与安全 handoff S3 同源，可合并实施。
- **波次 2（客户端主线，严格串行）**：harness-01 → harness-06 → harness-03，harness-04 在 01 后插入。都大改 `opencode_client.py`，必须一个接一个。
- **波次 3（独立项）**：harness-09（镜像/Skill 体系）、frontend-01、frontend-02。与波次 2 路径不重叠，可并行。

## 4. 全局约束（所有任务遵守）

- 分支：在 `Dev` 上开 `task/{id}`；提 PR 前先合最新 `Dev`（根 AGENTS.md 提交边界）。
- 配置分层：代码默认值保持本地安全；5090 实测取值只写 `code/docker-compose.5090.yml`。harness-02 对齐默认值时不得把本地默认改成 5090 取值。
- 双轨不越界：改共享底座（opencode_client、config、compose）时必须确认技术/商务两轨行为都不退化。
- 后端测试（与 CI 对齐）：
  ```bash
  cd code/sewpg-bid-backend
  APP_STORE_BACKEND=memory \
  DATABASE_URL="postgresql+asyncpg://biduser:bidpass@localhost:5432/bidplatform" \
  .venv/bin/python -m pytest -m "not integration" <相关测试文件>
  ```
- 前端改动至少 `npm run build` 冒烟；compose/Dockerfile 改动需确认服务可启动且健康检查通过。
- 模块行为有实质变更时，同步更新 `docs/anbc_doc/架构总览/modules/<name>.md` 卡片与 `_data/` 摘要。

## 5. 决策点（开工前需拍板）

| 决策点 | 影响任务 | 现状 |
|---|---|---|
| coverage/export 后端端点保留还是删除（前端「有意不接」是架构决策，见 `src/workspaces/README.md:15`） | deadcode-04 | 倾向保留端点、只删前端悬空封装，handoff 内附确认清单 |
| tag-import 是否被 Skill 直调后端 | deadcode-04 | 需 grep opencode/skills 确认 |
| 项目信息选项的真实数据源是什么（素材库客户目录？手工维护表？） | frontend-02 | 未定，未拍板前不动工 |
| 质量警示的展示形态（逐条徽标 vs 详情区警示条） | frontend-01 | handoff 给了建议方案，可实施时微调 |

## 6. 不做的事（明确排除）

- 不做 20260718 安全 handoff 的 S1（全量 API 鉴权）等安全项——harness-05 只覆盖 opencode 端口，其余安全项不在本 plan 范围。
- 不顺手重构与清单无关的模块；文档更正（00/04/05、模块卡片、_data）已完成，不在任务内。
