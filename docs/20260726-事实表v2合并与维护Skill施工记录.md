# 技术标事实表 v2 合并 + 维护 Skill 施工记录（2026-07-26）

> 性质：本轮工作梳理 / 交接记录
> 分支：`feature/project-fact-table-20260723`（已 rebase 至 origin/Dev 33a13c0，**仅本地，未 push**）
> 关联文档：`docs/20260723-项目事实表填写任务梳理.md`、`docs/20260726-技术标事实表自动触发与维护Skill方案.md`

---

## 一、本轮做了什么（按时间线）

### 1. 代码检查与镜像重建

- 本地预检：代码落后 origin/Dev 33 个提交（必检项不通过），工作区干净
- `git pull --ff-only` 对齐后重建 `fastapi` / `web` / `opencode` 三镜像，健康检查全过

### 2. 排查"事实表只有 12 个字段"

- 现象：PRJ-0007 技术标事实表仅 12 字段
- 结论：不是 bug 也不是写死——Dev 上的 v1 构建逻辑是"各来源候选并集"，该项目素材未入库、fillTasks 未生成、解析白名单命中少，只有基础字段
- 对照 `todo-技术标项目事实表清单-0722.xlsx`（148 字段）与对照文档，确认设计预期是 62 个单值字段进事实表

### 3. 找回 v2 实现并合入

- 发现完整 v2 实现（148 清单 spec、专项抽取器、七态模型、前端逐字段确认）在本地分支 `feature/project-fact-table-20260723` 的 8 个提交里，**从未合入 Dev**（7/25 只摘走了 3 个修复提交走 PR #84）
- rebase 到最新 origin/Dev（33a13c0），零冲突；后端全量 pytest **1323 passed**
- 重建镜像后事实表 12 → **133 字段**（121 个清单 spec 骨架）

### 4. 打通素材抽取链路（两个真实缺陷）

- **素材不可见**：风资源报告、塔架工程量、弯矩表、承诺函×2、基地专题×2 共 7 个素材原挂在废弃项目目录，已移至 `技术标/项目定制/{PRJ-0007 项目名}`；移动后发现文件 tier 元数据仍是 `standard`（API 建的文件夹 tier 一律存 standard，不按路径推断——**产品缺陷，未修代码，已直接改库修正这 7 个文件**）
- **事件循环 bug（真 bug，已修）**：`facts/build` 是 async 端点，同步构建在事件循环线程内跑，素材查询经 `run_awaitable_sync` 桥接被拒抛错，又被 `except Exception: materials = []` 静默吞掉 → API 下素材永远为空
  - 修复：`technical_gap_service.py` 两处构建调用改 `asyncio.to_thread`；`technical_gap_fact_table.py` 静默 except 加 `logger.exception`
- 修复后：**88 字段自动提取 + 9 待确认**，仅剩 35 个"清单-招标文件"类未提取

### 5. 方案设计

- 输出方案文档：`docs/20260726-技术标事实表自动触发与维护Skill方案.md`
- 三层锚定：清单（模板级，一次入库）/ 素材（按 standard/customer/project 分层复用）/ 触发（自动化）
- 方案 A（代码自动触发，T1~T4，未动工）；方案 B（OpenCode 维护 Skill，T5~T6，本轮施工）

### 6. 方案 B 施工：bid-tech-fact-curator（已完成）

- 新增 skill `opencode/skills/bid-tech-fact-curator/`（SKILL.md + references/rules.md + scripts/run_from_manifest.py 证据简报生成器）
- 新增后端 `app/services/technical_fact_curator.py`（manifest 组装 / opencode 调用隔离 / 建议回收）
- `opencode_client.py` 加 `run_bid_tech_fact_curator_with_trace`；`technical.py` 加 `POST .../gaps/facts/curate`；STAGES.md、opencode/Dockerfile（`factcurate` 别名）接入
- 回收硬约束：建议一律 `pending_confirmation`、绝不写/覆盖 `confirmed`、无值保持 `unextracted` + notes
- 独立评审通过（89 测试全绿，3 条非阻塞遗留，见 §三）

### 7. 端到端验证（PRJ-0007，真实 LLM）

- 首跑：LLM 产出 68 条建议（27 条带值），但**落表 0 条**——brief 的 fieldKey 与表字段真实 key 不一致（别名/大小写），全部匹配落空
- 修复：manifest 透传真实 key、SKILL.md 铁律"逐字照抄 fieldKey"、回收侧防御性归一匹配；顺带修非法 action 静默降级、补 opencode 失败 400 用例；36 测试全绿
- 用同一份真实建议回放：**28 补抽 + 1 修正 + 3 口径建议全部落表**，9 个 confirmed 字段未被触碰

## 二、当前状态（PRJ-0007 事实表）

| 状态 | 数量 | 说明 |
|---|---|---|
| confirmed | 9 | 人工已确认 |
| extracted | 65 | 代码自动提取 |
| pending_confirmation | 38 | 含 32 个带 factCurator 证据的 AI 建议，待人工逐条确认 |
| unextracted | 29 | AI 已写明原因在 notes：可利用率/功率曲线保证率等数值在招标文件第二卷技术规范书，未解析 |

下一步用户动作：事实表弹窗逐条确认 38 个待确认字段 → 整表 confirmed → AI 填写放行。

## 三、2026-07-27 追加：清单驱动的素材定向读取 + 齐备性预检 + 一个竞态真 bug

> 分支同上，代码仍全部在工作区未提交。动机：多项目规模化后新项目素材未挂导致事实表空转（PRJ-0008 曾 117/139 未提取）。

### 本轮施工（计划：清单 referenceFile 为唯一指路牌，跨项目素材只进 AI 建议通道）

- **T1 素材类别映射**（新增 `app/services/technical_fact_material_classes.py`）：从 spec `referenceFile` 归一出 6 类素材（wind_resource 26 字段 / tower_quantity 38 / bending_moment 18 / hours_commitment 1 / production_base 4 / cert 4；清单原文 typo "基础弯矩表表" 按"弯矩"子串归一）；`classify_material` 按文件名/folderPath 归到同类别；`material_home_project` 从 folderPath 解析归属项目
- **T2 齐备性预检 API**：`GET /api/technical/projects/{id}/gaps/facts/material-check`，本项目范围素材按类对账，缺失类别扫全库「技术标/项目定制」给跨项目候选（仅元数据，每类上限 5）
- **T3 curator manifest 定向增强**：素材带 `materialClass`/`homeProject`/`crossProject`；fields 按 specKey（specSeq 兜底）关联 spec 补 `referenceFile`/`materialClass`；缺失类别注入跨项目候选（每类 ≤3，占 `_CURATOR_MATERIAL_LIMIT` 额度，本项目素材优先）；回收侧跨项目证据 notes 追加「跨项目来源：{项目}/{素材}」
- **T4 SKILL 规则**：铁律 +2 条——按字段 materialClass 定向找素材、referenceFile 为招标文件的只从 tenderSources 取数；crossProject 素材必须核对项目名/场址/机型一致才可给值，拿不准给 `action:"confirm"`
- **T5 测试**：新增 `test_fact_material_classes.py`（10）、`test_fact_material_check.py`（6），`test_fact_curator.py` 增补，目标测试 36 条全绿；全量回归未跑（容器内 pytest 环境性卡顿，建议正式环境补跑）

### 端到端实测（PRJ-0008 / PRJ-0007）

- PRJ-0008 material-check：缺 5 类（wind_resource/tower_quantity/bending_moment/hours_commitment/production_base，影响 87 字段），跨项目候选正确给出（含 PRJ-0007 的 7 份素材，归属项目标注正确）；cert 类命中 17 份
- PRJ-0007 material-check 回归：六类全齐、零缺失
- **首次 curate 落 0 条 → 抓出潜伏竞态 bug（本轮已修）**：`opencode_client` 的提前返回在 `factcurate` 脚本（只生成 brief）完成时就 return，而建议文件 outputFile 是 LLM 之后才写的——后端读不到文件按 0 条回收（7/26 成功只是赢了竞态）。修复：新增 `early_tool_wait_file` 参数，建议文件落地前不提前返回（`run_technical_fact_curator_skill` 透传 manifest 的 outputFile）。补 2 条测试，`test_fact_curator.py + test_opencode_client.py` 81 条全绿
- 修复后重跑 PRJ-0008 curate：LLM 产出 42 条建议，落表 **17 补抽 + 1 修正**（23 未找到值、1 忽略）；事实表 22 extracted/117 unextracted → 21 extracted + **18 pending_confirmation** + 100 unextracted。18 条待确认中 **11 条带「跨项目来源」标注**（均来自 PRJ-0007 的风资源报告/承诺函等，数值如 2917 小时、净发电量，须人工确认两项目是否同标的才可采信）——跨项目只进建议通道的设计按预期生效，无任何值绕过人工落表

### 遗留事项新增

| # | 事项 | 出处 |
|---|---|---|
| 9 | 全量 pytest 回归未跑（容器内 pytest 9.x 卡 test_parse_async_jobs、8.3.5 下该文件 7 条环境性失败，与本轮改动无交集）；正式环境补跑 | 本轮 |
| 10 | 预检结果暂无前端面板，只有 API（`GET .../facts/material-check`） | 本轮 |

### 既有遗留事项（1~8，仍有效）

| # | 事项 | 出处 |
|---|---|---|
| 1 | 方案 A（自动触发 T1~T4：build 后台化+防抖、识别完成触发、素材写操作触发、完整率面板）未动工 | 方案文档 §六 |
| 2 | T7：商务标 `build_facts`/`save_facts` 同构事件循环 bug 未修（本次按边界只改技术标） | 方案文档 §六 |
| 3 | 评审遗留（非阻塞）：opencode_client 新方法无直接单测；非法 action 已改计入 ignored✅（修复时已做）；opencode 失败 400 用例✅（修复时已做） | 评审报告 |
| 4 | 29 个 unextracted 字段需补招标文件第二卷技术规范书解析，或人工填写 | 端到端实测 |
| 5 | API 新建文件夹 tier 恒为 standard 的产品缺陷（素材移动后需手工刷 tier） | §4 |
| 6 | 分支 `feature/project-fact-table-20260723` 未 push、未发 PR；工作区尚有未提交改动（本轮全部代码改动） | 本轮 |
| 7 | `curateReport.ignored` 结构由字符串改为 `{fieldKey, reason}`，前端如消费需适配（当前未消费） | 修复备注 |
| 8 | **重建会冲掉 curator 建议**：`facts/build` 只保留 confirmed/人工结果，`pending_confirmation` 的 AI 建议及其证据、unextracted 的 notes 均被重置。2026-07-27 02:28 UTC PRJ-0007 被从前端重建一次，上一轮 32 条带证据建议即因此丢失（仅 6 条幸存）。要么产品上避免随意重建，要么改 build 保留 pending 建议 | 本轮实测 |

## 四、关键文件索引

- 事实表构建：`app/services/technical_gap_fact_table.py`（v2，1619 行）
- 字段 spec：`app/services/technical_fact_field_specs.py` + `app/data/technical_fact_field_specs.json`（148 条，128 条需填值）
- 素材类别映射/齐备性预检：`app/services/technical_fact_material_classes.py`（清单 referenceFile 驱动）
- 专项抽取器：`app/services/technical_fact_special_extractors.py`（风资源/塔架工程量/弯矩/证书/承诺函/基地）
- curator：`app/services/technical_fact_curator.py`、`opencode/skills/bid-tech-fact-curator/`
- 测试：`tests/test_fact_curator.py`、`test_fact_material_classes.py`、`test_fact_material_check.py`、`test_fact_table_downstream.py`、`test_fact_field_level_confirm.py`
- API：`POST /api/technical/projects/{id}/gaps/facts/build`、`.../facts/curate`、`GET .../gaps/facts/material-check`、`GET/PUT .../gaps/facts`、`PATCH .../gaps/facts/{field_id}`
