# 甲方新增需求待办

> 日期：2026-04-29
> 说明：以下为本轮确认需求，模型问题暂不处理。
> 排序口径：2026-05-01 调整为按**实施难度由低到高**排序，便于先打通最小闭环、再吃硬骨头。原编号 4 拆为 4-A（UI/库）和 4-B（解析），独立推进。

## 待办清单（按实施难度低→高）

### 1. [难度：低] 创建项目改为起止日期（UI/库部分） — 原编号 4-A

- [ ] 创建项目模态把"截止日期"改为"起始日期 + 截止日期"两个字段。
- [ ] 后端项目 store/model 增加 `start_date` 字段，保留 `end_date`。
- [ ] 项目列表/详情页同步展示起止日期。
- [ ] 字段允许人工编辑（不依赖招标文件解析）。

**关键文件**：
- `code/sewpg-bid-frontend/src/components/modals/ProjectWizardModal.jsx`
- `code/sewpg-bid-backend/app/services/store.py`
- `code/sewpg-bid-backend/app/api/routes/projects.py`

### 2. [难度：中-低] OnlyOffice 左右布局与全屏 — 原编号 6

- [ ] 所有使用 OnlyOffice 的页面统一为左右布局。
- [ ] 左侧放目录、素材、审核项或上下文信息。
- [ ] 右侧放 OnlyOffice。
- [ ] 提供 OnlyOffice 全屏显示开关。

**当前现状**：`OutlineReview.jsx` 已有 grid 2 列；`MaterialReview.jsx`、`CoCreationEditor.jsx` 等需补齐。

**关键文件**：
- `code/sewpg-bid-frontend/src/pages/OutlineReview.jsx`
- `code/sewpg-bid-frontend/src/pages/MaterialReview.jsx`
- `code/sewpg-bid-frontend/src/pages/CoCreationEditor.jsx`

### 3. [难度：中] 人工指定机型后选择素材 — 原编号 5

- [ ] 素材库素材模型增加 `model_type`（机型）字段。
- [ ] 从素材库选择投标素材时，先由人工选定机型，再按机型筛选素材。
- [ ] 避免自动选到不匹配机型的素材。

**关键文件**：
- `code/sewpg-bid-backend/app/services/material_store.py`
- `code/sewpg-bid-backend/app/models/materials.py`
- `code/sewpg-bid-frontend/src/components/modals/MaterialSelectModal.jsx`
- `code/sewpg-bid-frontend/src/pages/GapFilling.jsx`

### 4. [难度：中] 素材库清洗稿 OnlyOffice 预览 — 原编号 1

- [ ] 素材上传后继续走清洗流程（已有）。
- [ ] 清洗完成后，用 OnlyOffice 预览清洗后的 Word。
- [ ] 未清洗完成或清洗失败时，不开放预览。

**当前现状**：`material_cleaning.py` 与前端 `cleanStatus`、`cleanedDownloadUrl` 已具备，仅缺 OnlyOffice 预览接入；建议依赖第 2 项左右布局完成后再做以复用骨架。

**关键文件**：
- `code/sewpg-bid-backend/app/services/material_cleaning.py`
- `code/sewpg-bid-frontend/src/pages/MaterialDB.jsx`

### 5. [难度：中-高] opencode 完成/失败邮件通知 — 原编号 2

- [ ] opencode 任务处理完成后自动发送邮件通知。
- [ ] opencode 任务失败时也发送失败通知。
- [ ] 通知对象优先为项目负责人、任务发起人或审核人。

**当前现状**：仓库未发现 SMTP/邮件配置，需新增邮件服务层与配置项；触发点接到 worker 任务完成/失败回调。

**关键文件**：
- `code/sewpg-bid-backend/app/workers/redis_worker.py`
- `code/sewpg-bid-backend/app/core/config.py`
- 新增邮件服务模块（建议 `app/services/notification.py`）

### 6. [难度：高] 起止日期从招标文件读取 — 原编号 4-B

- [ ] S1 解析阶段从招标文件中识别起止日期。
- [ ] 解析结果写入项目字段，作为创建项目时的默认值。
- [ ] 仍允许人工编辑覆盖。

**当前现状**：解析层仅做文本抽取，未做结构化日期识别；可能需 LLM 辅助抽取或基于关键词正则的兜底。依赖第 1 项 UI/库已落地。

**关键文件**：
- `code/sewpg-bid-backend/app/services/parsing.py`
- `code/sewpg-bid-backend/app/api/routes/parse.py`

### 7. [难度：高] 导出前刷格式与交叉引用 — 原编号 7

- [ ] 最终导出前增加一步 opencode 处理。
- [ ] 调用 skill 对 Word 刷格式。
- [ ] 支持索引、表格编号和交叉引用。
- [ ] 刷格式完成后再导出最终文件。

**当前现状**：S7 拼装侧已落地"素材内部 Heading 降级 + 主导航修复"作为前置基础（commit `fix(s7): keep material sub-headings out of word navigation`），仍需新增导出阶段 skill 与 S10 流程串接。

**关键文件**：
- `code/sewpg-bid-backend/app/api/routes/export.py`
- `code/sewpg-bid-backend/app/services/tech_assembly.py`
- 新增 `opencode/skill/bid-export-formatter/`（待建）

### 8. [难度：高] S7 改为 Agent 决策素材匹配（架构调整）

- [ ] backend 停止预算"素材→目录项"匹配，去掉卡片 `skeleton_section` 注入。
- [ ] 改为 agent 浏览 wiki 卡片自主决策，产出 `assembly_plan.json`，再交 merger 拼装。
- [ ] 硬约束：agent 只决定匹配，不写正文（契合甲方"怕自由发挥"）。
- [ ] 加 plan JSON Schema 校验 + 失败回退到旧 keyword 路径，env flag 灰度。

**当前现状**：当前 skill 是脚本伪装 agent——backend 用关键词子串预算匹配，prompt 明确写"不要思考，直接执行脚本"，agent 自由度 0%。本项是把 S7 真正 agent 化。

**关键文件**：
- `code/sewpg-bid-backend/app/services/tech_assembly.py`
- `code/sewpg-bid-backend/opencode/skill/bid-tech-assembler/SKILL.md`
- `code/sewpg-bid-backend/opencode/skill/bid-tech-assembler/scripts/build_assembly.py`

## 模型问题

- [ ] 本轮暂不处理。

## 现存 Mock 清理

- [ ] 登录鉴权：去掉 mock token、固定用户和前端默认账号密码。
- [ ] S4/S5/S6：缺口识别、补料、审核改为读取真实素材库和真实补料记录。
- [ ] 结构化素材导入：Excel 预览、校验、确认导入改为真实解析，不再返回样例行。
- [ ] 系统设置：用户、LLM 网关测试、备份、健康状态改为真实数据或真实探测。
- [ ] 审计日志：改为记录真实操作，不再使用内存 fixture。
- [ ] S1 解析结果：补齐结构化抽取 items，不只做文本抽取。
- [ ] S10 导出检查：一致性、格式检查、PDF 导出改为真实能力。
- [ ] 清理旧 mock/demo 资产：前端 `mock-server`、`legacy/fastapi-mock`、后端 OnlyOffice/opencode demo。
