# 甲方新增需求待办

> 初始日期：2026-04-29
> 更新日期：2026-05-02
> 来源：
> - 本轮甲方确认新增需求
> - [15-技术标与商务标需求整理.md](/Users/wlb/Agent/bid-project/doc/15-技术标与商务标需求整理.md)
>
> 排序口径：按实施难度由低到高。同一业务事项合并为一条，不再拆 A/B。
> 推进口径：后续按本清单逐项推进；完成一项就在“完成情况”列勾选，并同步记录到 `code/progress.md`。
> 会话口径：后续开始做待办时，默认每个待办开一个新会话；新会话先读本文件、`code/progress.md` 和 `code/AGENT.md`。

## 待办清单

| 序号 | 完成情况 | 难度 | 待办 | 范围 |
|---:|---|---|---|---|
| 1 | [x] | 低 | 清理旧 mock/demo 资产 | 清理前端 `mock-server`、`legacy/fastapi-mock`、后端 OnlyOffice/opencode demo 等不再进入当前运行路径的旧资产。 |
| 2 | [x] | 中-低 | OnlyOffice 左右布局与全屏 | 所有 OnlyOffice 页面统一左侧上下文、右侧文档；提供全屏开关。 |
| 3 | [x] | 中-低 | 模板上传 Fallback 读取 | 模板上传界面支持设置 fallback 模板来源；如果项目不上传模板文件，后续目录/正文生成可直接从 fallback 读取模板。 |
| 4 | [ ] | 中 | 人工指定机型后选择素材 | 素材增加机型字段；选择素材前先选机型，再按机型筛选，避免错配。 |
| 5 | [x] | 中 | 素材库清洗稿 OnlyOffice 预览 | 素材清洗完成后可用 OnlyOffice 预览清洗稿；清洗失败或未完成时不开放预览。 |
| 6 | [ ] | 中 | 系统设置真实化 | 用户、LLM 网关测试、备份、健康状态改为真实数据或真实探测。 |
| 7 | [ ] | 中 | 审计日志真实化 | 审计日志记录真实操作，不再使用内存 fixture。 |
| 8 | [x] | 中-高 | 项目起止日期 | 创建项目支持起始日期和截止日期；项目列表/详情展示；S1 后续从招标文件识别起止日期并允许人工覆盖。 |
| 9 | [ ] | 中-高 | opencode 完成/失败邮件通知 | opencode 任务完成或失败后发送邮件，通知项目负责人、任务发起人或审核人；支持 SMTP 配置。 |
| 10 | [ ] | 中-高 | 结构化素材导入真实化 | Excel 预览、校验、确认导入改为真实解析，不再返回样例行。 |
| 11 | [ ] | 中-高 | 登录鉴权真实化 | 去掉 mock token、固定用户和前端默认账号密码，接入真实登录鉴权。 |
| 12 | [x] | 高 | 工作流收敛：目录生成、缺口处理、校验合并 | 模板上传后按钮直接变为“生成目录”，在当前页显示 opencode/Skill 进度和输出；生成后直接进入目录审核。去掉独立目录生成页和备料页；缺口识别页改为“缺口识别与处理”，由 opencode/Agent Skill 判断已确认目录是否能在素材库 Wiki 中匹配到素材，并产出可审核的匹配/缺口计划。缺口项可直接上传客户资料或调用 Skill 填写招标附表空表，填写后用 OnlyOffice 预览。校验能力内嵌为缺口页状态或“重新检查缺口”，全部缺口解决后直接生成标书并进入共创。 |
| 13 | [x] | 高 | 解析页面多招标文件 Skill 解析 | `http://127.0.0.1/parse` 解析页面支持多个招标文件一起解析，例如总发包招标文件和子项目招标文件；点击解析时按需求文档触发专门的解析 Skill，输出评分细则、项目基础信息、风机参数、性能指标、环境适应性、专题方案等结构化结果，并保留文件来源和证据位置，供后续项目流程使用。 |
| 14 | [ ] | 高 | 技术标附表原样填充与缺失高亮 | 识别招标附表并保持业主表格结构不变，仅填充数据；无法填充项高亮提示并进入人工审核。 |
| 15 | [x] | 高 | S4/S5/S6 真实化 | 缺口识别、补料、审核改为读取真实素材库和真实补料记录。 |
| 16 | [ ] | 高 | S8 评分点/证据/段落覆盖审计 | 从“素材是否拼上”升级为评分点、资格要求、技术要求、证明材料和正文段落的覆盖追溯。 |
| 17 | [ ] | 高 | S10 导出前格式、检查和 PDF 能力 | 导出前刷新标题样式、目录、页码、页眉页脚、表图编号、交叉引用；技术标目录应尽可能完整；一致性/格式检查和 PDF 导出改为真实能力。 |
| 18 | [ ] | 高 | 商务标附件映射规则 | 识别承诺、通用资格条件、专用资格条件、资格审查资料，并映射到附件7、附件7I、附件9等模板位置。 |
| 19 | [ ] | 高 | 商务标否决项/符合性审查风险识别 | 识别符合性审查标准表和“否决”条款，输出必须满足或响应的高风险清单。 |
| 20 | [ ] | 高 | 商务评分索引与页码回填 | 识别商务评分标准表，生成封面后评分索引，并在定稿后回填“详见 XX 章节及 XX 页码”。 |
| 21 | [ ] | 高 | 商务标业绩清单自动匹配 | 根据素材库业绩清单，自动匹配招标文件模板中的业绩表，人工确认后写入。 |
| 22 | [ ] | 高 | OCR / 图片型 PDF 内容识别与人工复核 | 考虑加入 OCR 模型能力，环境变量预留 OCR API Key 入口；识别图片或 PDF 中可填入技术标/商务标的内容，先进入候选字段，人工确认后写入。 |

## 待确认事项

| 序号 | 事项 |
|---:|---|
| 1 | 商务标相关细化口径后续再确认。 |

## 待办 12/15 联合改造计划

> 记录时间：2026-05-02
> 状态：已实施。待办 12 和 15 已按联合改造口径完成，后续模型效果和复杂样本质量调优继续在相关专项待办中推进。

### 目标流程

1. 解析页面先从招标文件中解析评分细则、项目基础信息、风机参数、性能指标、环境适应性、专题方案、附表空表等结构化结果，并完成是否投标决策。
2. 决定投标后进入技术标工作流，第一步上传投标模板；如项目未上传模板，则 fallback 到系统通用模板。
3. 调用目录生成 OpenCode Skill。目录生成输入为：素材库 Wiki、解析后的招标文件结构化结果、项目上传或 fallback 的投标模板。
4. 人工审核并确认目录后，进入“缺口识别与处理”。
5. “缺口识别与处理”调用新的 OpenCode Skill，根据已确认目录、素材库 Wiki、真实素材库、解析结果和已有补料记录，生成可审核的匹配/缺口/处理计划。
6. 缺口项支持直接上传客户资料，或调用 OpenCode Skill 进行 AI 填写。AI 填写时由人工指定需要填写的空表/Word，以及可参考的素材库文件、解析字段或补料文件。
7. AI 填写产物作为项目补料结果入库，并可用 OnlyOffice 预览；无法填写的字段高亮或进入人工确认。
8. 所有缺口解决后，在缺口页内执行“重新检查缺口/完整性校验”。
9. 校验通过后直接进入标书生成。标书生成 Skill 按人工确认后的目录和缺口处理计划，使用原始素材、人工补料和 AI 填写产物拼接完整技术标正文，随后进入共创阶段。

### 核心中间产物

新增统一计划数据，作为 S3 目录审核之后、S7 标书拼接之前的唯一桥梁，可存为 `gap_plan.json` 或项目状态中的 `gapPlan`。

计划至少包含：

- 目录项：`tocItemId`、`number`、`title`、父章节、目录审核版本。
- 状态：`matched`、`missing`、`needs_input`、`filling`、`resolved`、`ignored`。
- 已匹配素材：素材 ID、素材库层级、Wiki 卡片、清洗稿 Word、匹配理由、置信度。
- 缺口原因：无素材、素材不适用、需要客户资料、需要填写附表空表、需要人工确认。
- 处理动作：上传补料、选择已有素材、AI 填写、忽略并说明原因。
- AI 填写任务：空表/Word 来源、参考素材、可用解析字段、输出文件、未填字段、高亮信息。
- 解决产物：人工上传文件、AI 填写文件、OnlyOffice 预览会话、是否可进入 S7 拼接。
- 校验结果：缺口是否清零、仍需人工确认的项、阻断标书生成的原因。

### 后端改造

1. 新增缺口识别 Skill，例如 `bid-tech-gap-planner`。
   - 输入：已确认目录 JSON、招标解析结构化结果、素材库 Wiki、真实素材库索引、项目身份、已有补料记录。
   - 输出：匹配/缺口/处理计划 JSON，不直接生成正文 Word。
2. 新增 AI 填写 Skill，例如 `bid-tech-table-filler` 或 `bid-appendix-filler`。
   - 输入：待填写空表/Word、人工指定的参考素材、招标解析字段、项目参数和填写约束。
   - 输出：填好的 Word/表格文件、未填字段清单、证据引用和高亮提示。
3. 改造 S4/S5/S6 后端接口。
   - `run_gap_detection` 不再从目录叶子节点伪造缺口，改为准备 manifest 并调用 `bid-tech-gap-planner`。
   - 补料上传真实写入项目素材库，不再只生成 mock receipt。
   - AI 填写结果真实写入项目素材库或项目产物区，并挂回对应缺口项。
   - 审核/校验读取 `gapPlan`、真实补料记录和 AI 填写产物。
4. 改造 S7 标书生成。
   - `bid-tech-assembler` 增加 `gapPlanPath` / `assemblyPlanPath` 输入。
   - 优先按缺口页确认后的计划拼接，不再完全依赖固定 `skeleton_section` 规则临时猜素材。
   - Word 合并、Heading、finalize、verify 能力继续复用。

### 前端改造

1. 模板上传页承接目录生成入口。
   - 模板上传后按钮直接变为“生成目录”。
   - 当前页显示 OpenCode/Skill 进度、任务状态和原始输出。
   - 目录生成完成后直接进入目录审核。
2. 去掉独立目录生成页、备料页、审核页的主流程入口。
   - 老路由可保留兼容或重定向，避免历史链接失效。
3. 将缺口识别页升级为“缺口识别与处理”。
   - 展示目录项、匹配素材、缺口原因、处理动作、状态和校验结果。
   - 支持上传客户资料、选择已有素材、发起 AI 填写、忽略并填写原因。
   - 支持 AI 填写任务状态、产物 OnlyOffice 预览、未填字段提示。
   - 内嵌“重新检查缺口/完整性校验”。
   - 缺口全部解决后直接触发标书生成并进入共创。

### 完成标准

- 待办 12 完成标准：
  - 技术标工作流不再依赖独立目录生成页、备料页和审核页作为主路径。
  - 模板页可直接生成目录并展示 OpenCode/Skill 进度和输出。
  - 缺口页完成识别、补料、AI 填写、OnlyOffice 预览、重新检查和进入生成。
- 待办 15 完成标准：
  - 缺口识别读取真实目录、真实 Wiki、真实素材库和解析结果。
  - 补料记录、AI 填写产物、审核/校验结果都进入真实项目状态或项目素材库。
  - S7 标书拼接读取缺口处理计划，使用原始素材、人工补料和 AI 填写产物。

## 暂不处理

- 模型效果问题。
- 商务标细化口径暂不展开。
