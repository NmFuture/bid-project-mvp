# UI统一改造验收记录

> 范围：第一轮样板页只覆盖商务标链路，不改技术标专属页面。

## 2026-05-21 阶段2样板页收口

### 本轮页面

```text
code/sewpg-bid-frontend/src/pages/BusinessGapRecognition.jsx
```

对应访问地址：

```text
http://127.0.0.1:5173/workspace/business/projects/PRJ-0006/gaps
```

### 已完成内容

- 新增基础 UI 组件：

```text
src/components/ui/Button.jsx
src/components/ui/IconButton.jsx
src/components/ui/Badge.jsx
src/components/ui/Dialog.jsx
src/components/ui/Toolbar.jsx
src/components/ui/FileButton.jsx
src/components/ui/utils.js
```

- 将商务标素材匹配样板页的主要操作按钮统一为 `Button` / `IconButton` / `FileButton`。
- 将状态标签统一为 `Badge`，覆盖任务状态、事实表状态、素材预览模式、Wiki 标识、产物确认状态。
- 将页面顶部操作区、任务操作区、候选素材操作区、弹窗底部操作区统一为 `Toolbar`。
- 统一素材选择弹窗页签按钮、预览按钮、确认/取消按钮。
- 统一上传补料入口，避免页面内继续手写 `label` 伪按钮样式。
- 统一目录项和素材处理方式卡片的按钮结构，使样板页的按钮高度、圆角、文字层级更稳定。
- 修复商务标阶段跳转：

```text
素材匹配 -> /workspace/business/projects/:id/gaps
共创导出 -> /workspace/business/projects/:id/editor
```

- 修复商务标目录确认后的跳转：

```text
确认目录后固定进入商务标素材匹配页，不再因项目 currentStage 已推进而直接进入共创导出。
```

### 验证记录

已通过：

```text
npm run lint
npm run build
```

浏览器验证：

- 商务标素材匹配页可正常打开。
- 点击“素材匹配”阶段停留在 `/gaps`。
- 点击“共创导出”进入 `/editor`。
- 点击“指定素材库材料/模板”可打开素材选择弹窗。
- 弹窗按钮和页签正常显示。

截图：

```text
/private/tmp/sewpg-ui-shots/screenshot-1779294091581.png
/private/tmp/sewpg-ui-shots/screenshot-1779294296947.png
```

### 待你审核的问题

请重点看：

1. 商务标 S3 页面整体是否更统一，是否符合内部 B 端工具气质。
2. 页面顶部 5 个按钮的主次层级是否合理：

```text
刷新 / 维护项目事实表 / 生成缺口计划 / 生成正文 / 进入共创导出
```

3. 左侧商务目录是否过密，是否需要进一步压缩或增加分组筛选。
4. 任务卡中的“指定素材库材料/模板 / 标记忽略 / 上传补料”层级是否符合业务习惯。
5. 候选素材卡片的信息量是否过多，是否需要隐藏部分 Wiki 细节到展开态。

### 下一步建议

如果样板页方向通过，进入阶段3，优先迁移：

```text
MaterialDB.jsx
MaterialWiki.jsx
MaterialSelectModal.jsx
AuditDetailModal.jsx
```

`BusinessTenderReview.jsx` 当前仍复用技术标 `TenderReview.jsx`，暂不纳入本轮批量改造，等待商务/技术拆分后再处理。
