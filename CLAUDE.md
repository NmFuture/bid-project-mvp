@AGENTS.md

## Claude Code 专属

- 本文件只作为 Claude Code 入口，团队共享规则以 `AGENTS.md` 为准。
- 不要在这里复制 `AGENTS.md` 内容；确需新增团队规则时优先改 `AGENTS.md` 或 `.claude/rules/`。
- 个人本地偏好写入 `CLAUDE.local.md` 或 `.claude/settings.local.json`，不要提交。
